# Optional local convenience. The canonical runner starts its pinned binary.
services:
  registry:
    image: registry:3.1.1
    ports:
      - "127.0.0.1:5000:5000"
    environment:
      REGISTRY_STORAGE_DELETE_ENABLED: "false"
